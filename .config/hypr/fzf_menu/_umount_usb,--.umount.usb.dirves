#!/bin/bash
set -e

udiskie-info --all --output "{mount_path}" |
    grep -v "^$" |
    fzf -i --multi --exact --reverse --prompt="Eject USB:" --delimiter / --with-nth -1 --print0 | xargs -0 --no-run-if-empty -I {} udiskie-umount --detach {} >/dev/null 2>&1
