N=4

for i in {a..z}; do
    # Or a function, foo &
    (
        echo "starting task $i.."
        sleep $(( (RANDOM % 3) + 1))
    ) &

    if [[ $(jobs -r -p | wc -l) -ge $N ]]; then wait -n fi

done
