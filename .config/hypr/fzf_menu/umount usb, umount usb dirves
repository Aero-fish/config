#!/bin/bash
set -e

udiskie-info --all --output "{mount_path}" |
    grep -v "^$" |
    fzf --multi --exact --reverse --prompt="Eject USB:" --delimiter / --with-nth -1 |
    xargs --no-run-if-empty -I {} udiskie-umount --detach {} >/dev/null 2>&1
