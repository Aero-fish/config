return {
    settings = {
        -- pyright = { disableLanguageServices = true },
        python = {
            analysis = {
                -- diagnosticMode = "workspace",
                typeCheckingMode = "strict",
            },
        },
    },
}
